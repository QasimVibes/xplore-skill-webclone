export default function Postcard(props) {
    return (
        <>
            <div>
                <span>{props.heading}</span>
                <span>{props.number}</span>
            </div>
        </>
    )
}
